# CLAUDE.md

Guidance for agents working in this repository.

## Agent skills

### Issue tracker

GitHub Issues on `ferrinm/plate-overview`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical names, used verbatim — all already exist on the repo. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
